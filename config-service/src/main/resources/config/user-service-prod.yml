server:
  port: 8082

spring:
  datasource:
    url: jdbc:postgresql://${DB_HOST:postgres-user}:5432/userdb
    driver-class-name: org.postgresql.Driver
    username: ${DB_USER:userservice}
    password: ${DB_PASS:userservice}
    hikari:
      maximum-pool-size: 10
      connection-timeout: 30000
  jpa:
    database-platform: org.hibernate.dialect.PostgreSQLDialect
    hibernate:
      ddl-auto: update
    show-sql: false
    open-in-view: false

logging:
  level:
    org.springframework: INFO
    covey: INFO
